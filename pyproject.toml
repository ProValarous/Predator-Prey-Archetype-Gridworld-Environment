[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ppage"
version = "0.9.0b2"
description = "A deterministic, modular multi-agent predator-prey gridworld testbed for controlled MARL experiments"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
authors = [
    { name = "Muhammad Ahmed Atif" },
    { name = "Nehal Naeem Haji" },
    { name = "Muhammad Affan" },
    { name = "Areesha Kashif" },
    { name = "Musab Kasbati" },
    { name = "Afshad Yazdi Sidhwa" },
]
keywords = [
    "reinforcement-learning",
    "multi-agent",
    "marl",
    "gridworld",
    "predator-prey",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "gymnasium",
    "numpy",
    "pygame",
    "pyyaml",
]

[project.optional-dependencies]
# Neural-network baselines (DQN, AC, A2C, A3C) and their training scripts
# need PyTorch; the environment itself and the tabular baselines do not.
baselines = [
    "torch",
    "tensorboard",
]
dev = [
    "pytest",
    "pytest-cov",
    "black",
    "flake8",
    "pylint",
    "pre-commit",
]
docs = [
    "mkdocs",
    "mkdocs-material",
    "mkdocstrings[python]",
    "matplotlib",
    "imageio",
    "pillow",
]

[project.urls]
Homepage = "https://github.com/UHUMALAB/PPAGE"
Documentation = "https://uhumalab.github.io/PPAGE/"
Repository = "https://github.com/UHUMALAB/PPAGE"
Issues = "https://github.com/UHUMALAB/PPAGE/issues"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.black]
line-length = 88
target-version = ["py310", "py311", "py312"]
extend-exclude = '''
/(
    \.venv
    | \.new_venv
    | src/ppage/core
)/
'''
